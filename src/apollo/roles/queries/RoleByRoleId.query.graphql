query RoleByRoleId($id: Int!) {
  role(id: $id) {
    id
    name
    color

    permissions {
      ...PermissionToggle
    }

    members {
      ...UserAvatar
    }

    availableUsersToAdd {
      ...UserAvatar
    }
  }
}
