mutation UpdateServerRole($roleData: UpdateRoleInput!) {
  updateServerRole(roleData: $roleData) {
    role {
      ...Role

      permissions {
        ...PermissionToggle
      }
      members {
        ...RoleMember
      }
      availableUsersToAdd {
        ...UserAvatar
      }
    }

    me {
      id
      serverPermissions
    }
  }
}
