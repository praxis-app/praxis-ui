query GroupRoleByRoleId($id: Int!) {
  groupRole(id: $id) {
    id
    name
    color

    permissions {
      ...PermissionToggle
    }

    members {
      ...UserAvatar
    }

    availableUsersToAdd {
      ...UserAvatar
    }
  }
}
