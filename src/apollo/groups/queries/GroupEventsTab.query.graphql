query GroupEventsTab($groupId: Int!) {
  group(id: $groupId) {
    name
    upcomingEvents {
      ...EventCompact
    }
    pastEvents {
      ...EventCompact
    }
    myPermissions {
      manageEvents
      createEvents
    }
  }
}
