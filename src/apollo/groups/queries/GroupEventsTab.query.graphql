query GroupEventsTab($groupId: Int!, $isLoggedIn: Boolean!) {
  group(id: $groupId) {
    name
    upcomingEvents {
      ...EventCompact
    }
    pastEvents {
      ...EventCompact
    }
    myPermissions {
      manageEvents
      createEvents
    }
  }
}
