query GroupEventsTab($groupId: Int!) {
  group(id: $groupId) {
    name
    upcomingEvents {
      ...Event
    }
    pastEvents {
      ...Event
    }
    myPermissions {
      manageEvents
      createEvents
    }
  }
}
