query EditGroupRole($id: Int!) {
  role(id: $id) {
    ...Role
    ...AddMemberTab
    ...DeleteRoleButton

    permissions {
      ...PermissionsForm
    }

    availableUsersToAdd {
      ...UserAvatar
    }
  }
}
