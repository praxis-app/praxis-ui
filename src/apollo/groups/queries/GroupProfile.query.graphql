query GroupProfile($name: String!, $isLoggedIn: Boolean!) {
  group(name: $name) {
    ...GroupProfileCard

    feed {
      ...FeedItem
    }
  }

  me @include(if: $isLoggedIn) {
    id
    ...ToggleForms
  }
}
