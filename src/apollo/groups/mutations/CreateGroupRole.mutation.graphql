mutation CreateGroupRole($roleData: CreateGroupRoleInput!) {
  createGroupRole(roleData: $roleData) {
    role {
      ...Role

      group {
        id
        roles {
          ...Role
        }
      }
    }
  }
}
