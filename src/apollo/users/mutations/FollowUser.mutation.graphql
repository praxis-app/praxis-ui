mutation FollowUser($followedUserId: Int!) {
  followUser(followedUserId: $followedUserId) {
    followedUser {
      id
      ...UserProfileCard
    }
    follower {
      id
      ...UserProfileCard
    }
  }
}
