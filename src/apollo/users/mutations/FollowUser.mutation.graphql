mutation FollowUser($followedUserId: Int!) {
  createFollow(followedUserId: $followedUserId) {
    follow {
      id
      followedUser {
        id
        ...UserAvatar
        ...FollowButton
      }
    }
  }
}
