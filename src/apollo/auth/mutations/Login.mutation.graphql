mutation Login($input: LoginInput!) {
  login(input: $input) {
    user {
      id
      ...UserAvatar

      serverPermissions {
        ...UserServerPermissions
      }

      joinedGroups {
        id
        name
      }
    }
  }
}
