mutation Login($input: LoginInput!) {
  login(input: $input) {
    user {
      id
      ...UserAvatar

      serverPermissions {
        ...ServerPermissions
      }

      joinedGroups {
        id
        name
      }
    }
  }
}
