mutation SignUp($input: SignUpInput!) {
  signUp(input: $input) {
    user {
      id
      ...UserAvatar

      serverPermissions {
        ...UserServerPermissions
      }

      joinedGroups {
        id
        name
      }
    }
  }
}
