mutation SignUp($input: SignUpInput!) {
  signUp(input: $input) {
    user {
      id
      ...UserAvatar

      serverPermissions {
        ...ServerPermissions
      }

      joinedGroups {
        id
        name
      }
    }
  }
}
